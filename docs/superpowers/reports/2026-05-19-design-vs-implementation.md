# MacCleaner — 设计文档与实现差异对照

> 生成日期: 2026-05-19
> 对比范围: 设计文档 v1.0 vs 当前 master 分支

## ✅ 已完成

### 架构与基础设施
| 设计项 | 状态 | 备注 |
|--------|------|------|
| Electron + React + TS + Vite 架构 | ✅ | 完整实现 |
| Zustand 状态管理 | ✅ | |
| Tailwind CSS 深色主题 | ✅ | 已重构为 macOS 系统设置风格 |
| macOS Full Disk Access 权限 | ✅ | |
| 主进程 + 渲染进程 IPC 通信 | ✅ | 完整协议 |

### 双形态
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 菜单栏模式（Tray 弹出面板） | ✅ | 320x460 panel 窗口 |
| 完整窗口模式 | ✅ | 1152x712 |
| 面板与主窗口双向通信 | ✅ | `navigate:module` / `events:refresh` |

### 左侧导航
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 三栏布局（导航 + 列表 + 详情） | ✅ | 超出设计（设计为两栏） |
| 分类导航分组 | ✅ | 清理模块 / 卸载管理 / 设置 |
| 模块切换 + 快捷键 | ✅ | Cmd+1~9 |

### 清理模块
| 模块 | 状态 | 备注 |
|------|------|------|
| 仪表盘 | ✅ | 可清理总计 + 模块进度条 |
| Brew 缓存 | ✅ | 已安装包 + 旧版本 + 缓存大小 |
| Docker 清理 | ✅ | 悬空镜像 + 停止容器 + volume + build cache |
| npm/Node | ✅ | Node 版本列表 + npm cache |
| Conda/Python | ✅ | 环境列表 + pip cache |
| 系统缓存 | ✅ | 缓存目录列表 + 大小 |
| CLI 工具列表 | ✅ | 工具缓存目录扫描 |
| Downloads | ✅ | 文件列表 + 移至废纸篓 |

### 卸载模块
| 模块 | 状态 | 备注 |
|------|------|------|
| 已安装 APP 卸载 | ✅ | 关联文件扫描 + 隐藏目录匹配 |
| CLI 工具卸载 | ✅ | 支持 brew/npm 来源 |
| APP 残留清理 | ✅ | 扫描已卸载 APP 的残留 |

### AI 增强分析
| 设计项 | 状态 | 备注 |
|--------|------|------|
| Ollama 本地模型支持 | ✅ | 设置页开关 + 地址配置 + 测试连接 |
| AI 目录分析 IPC | ✅ | `ai:analyze` handler |
| 规则引擎兜底 | ✅ | AI 不可用时静默回退 |

### 自动化
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 每日定时扫描 | ✅ | cron 表达式，持久化到 `schedule.json` |
| 防重复触发 | ✅ | |
| 支持停止 | ✅ | |

### 安全策略
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 操作日志 | ✅ | 最多 200 条，持久化到 `operation-log.json` |
| 混合清理模式 | ✅ | 安全清理（绿色）+ 高级清理（橙色） |
| 卸载二次确认 | ✅ | 保留用户数据 / 完全清除 |
| 隐藏目录保护 | ✅ | `.ssh` 等目录排除 |

### 窗口行为
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 关闭隐藏到 Tray | ✅ | 非退出 |
| 单实例锁 | ✅ | |
| 开发/生产 DevTools | ✅ | |
| Cmd+R 拦截 | ✅ | 受设置控制 |
| Panel 失焦隐藏 | ✅ | |

### 自动更新
| 设计项 | 状态 | 备注 |
|--------|------|------|
| electron-updater + GitHub Releases | ✅ | 设计文档中无此项（后续新增） |
| 启动自动检查 | ✅ | |
| Toast 通知 | ✅ | 设计文档中无此项（后续新增） |

### 关于面板
| 设计项 | 状态 | 备注 |
|--------|------|------|
| 独立 BrowserWindow | ✅ | 原生红绿灯 + 拖拽 |
| 自定义图标 | ✅ | sips 转 PNG 方案 |
| 应用菜单入口 | ✅ | `关于 MacCleaner` + `检查更新` |

### UI 组件（设计外）
| 组件 | 状态 | 备注 |
|------|------|------|
| AutoHideScroll | ✅ | macOS 风格滚动条 |
| CollapsibleFileSection | ✅ | 可折叠文件列表 |
| SelectionSummary | ✅ | 多选汇总 |
| ErrorBoundary | ✅ | 错误边界 |
| LayoutWrapper | ✅ | 布局管理 |
| FinderIcon | ✅ | 访达图标 |
| Toast | ✅ | Toast 通知 |

## 📋 设计文档已实现但 UI 有差异

| 设计项 | 实现差异 |
|--------|----------|
| 左侧导航分区 | 设计为纯文本分组，实际用图标 + 圆角按钮 |
| 仪表盘布局 | 设计为卡片式，实际为进度条列表 + 可清理总计 |
| 设置页 | 设计为简单开关列表，实际为卡片式分组 |
| 快捷键 | 设计为全局快捷键，实际主进程拦截 + 设置页开关 |

## ⚠️ 未实现（设计文档有但代码无）

### 功能缺失
| 设计项 | 优先级 | 说明 |
|--------|--------|------|
| **AI 云端 Claude API 支持** | P2 | 设计 6.3 节提到双模型（Ollama + Claude API），目前仅实现 Ollama |
| **面板一键安全清理全部** | P2 | 设计 4.1 节仪表盘有"安全清理全部"快捷按钮，Tray 面板应支持 |
| **隐藏目录分类表前端展示** | P3 | 设计第 7 节，设计文档仅作为参考，无需单独页面 |

### 文档缺失
| 设计项 | 说明 |
|--------|------|
| **README.md** | 项目文档缺失 |
| **设计文档状态更新** | 当前设计文档状态为"待审批"，需更新为"已部分实现" |
| **API 文档** | IPC 协议文档未同步最新变更 |

## 📝 需要更新的文档

1. `docs/superpowers/specs/2026-05-13-maccleaner-design.md`
   - 状态改为"已部分实现"
   - 补充第 14 节：自动更新 + Toast 通知
   - 补充第 15 节：实现差异总结
   - 更新目录结构（第 10 节）

2. 新增 `README.md`
   - 项目介绍、功能特性、安装方法、开发指南

3. 更新设计文档中的目录结构（第 10 节）
   - 当前文件结构与原始设计有差异
